flask>=3.0
gunicorn>=21.0
requests>=2.31
# #587 : transport SSH (RouterOS CLI), comme le module Cisco.
paramiko>=3.4
# A1 (#620) : vérification du jeton Keycloak (RS256 / JWKS) pour le périmètre de site
PyJWT==2.9.0
cryptography==43.0.1
